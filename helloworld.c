#include <stdio.h> // library

int main() // every program needs a "main" function
{
    printf("Hello World\n"); // print out "Hello World" followed by a newline, to the console (terminal)

    return 0; // every main program needs to return 0 for indication of a successful run
}